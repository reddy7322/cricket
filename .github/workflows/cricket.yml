name: Auto Update Cricket Playlist

on:
  schedule:
    - cron: "*/15 * * * *"   # Runs every 15 minutes
  workflow_dispatch:         # Allows manual run

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          pip install requests

      - name: Generate cricket.m3u
        run: |
          python generate_cricket_m3u.py
          echo "==== FILE LIST ===="
          ls -lah
          echo "==== cricket.m3u CONTENT ===="
          head -n 20 cricket.m3u # Shows first 20 lines for safety

      - name: Commit & Push if changed
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          git add cricket.m3u

          if git diff --cached --quiet; then
            echo "No changes detected"
          else
            git commit -m "Auto update cricket.m3u ($(date -u))"
            git push
          fi
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          git add cricket.m3u

          if git diff --cached --quiet; then
            echo "No changes detected"
          else
            git commit -m "Auto update cricket.m3u ($(date -u))"
            git push
          fi
